# Optional. The app automatically reads the domain from the browser's URL bar
# for all SEO tags (canonical, Open Graph, JSON-LD). Set VITE_SITE_URL only if
# you want to pin it explicitly — it is still used at build time by
# `npm run sitemap` to generate sitemap.xml and robots.txt.
VITE_SITE_URL=https://your-domain.com
